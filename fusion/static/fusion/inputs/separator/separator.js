/* =============================================
 * Add Text Input
 * 
 * addInput(id, getValue, setValue, initInput);
 * ============================================= */
webrock.addInput(
        'separator',
        function($field) {
            // getValue
        },
        function($field, value) {
            // setValue
        },
        function($field) {
            // initInput
        }
)
