class Config:
    
    def data(self):
        config = {
            'name':'Responsive Grid',
            'icon': 'fa fa-th',
            'tags': 'grid, bootstrap, column, row',
            'preview': True
        }
        return config